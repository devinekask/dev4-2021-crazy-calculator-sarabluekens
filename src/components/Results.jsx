import React from 'react';

const Results = () => {
    return (
        <section className = "results">
            <h1 className="results__title"> Resultaten:</h1>
            <p className="results__title--Sub"> The demon is still deciding how to kill you.</p>
        </section>
    );
};

export default Results;